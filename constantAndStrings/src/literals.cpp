#include "literals.h"
#include <iostream>
void literals() {
	// we use suffixes with listeral(valuse we add directaly)
	// we will use most of the time f, as it required to defin float. the other is not important (maybe the s or sv)

	float fl{ 5.5f };
	std::cout << fl;
}