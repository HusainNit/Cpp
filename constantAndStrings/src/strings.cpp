#include "string.h"

void strings() {
	// c-styled string: is const that will live the enter life program, ist have null temination in the end to know the end of the string std::cout <<"hello";
	// unlike the std::string or std::string_view they will create temp object that will be terminated in end of its own experison
	
}